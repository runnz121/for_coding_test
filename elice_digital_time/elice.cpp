#include <bits/stdc++.h>

using namespace std;

int main(){

    std::ios_base::sync_with_stdio(false);
    cin.tie(0);

    int n;
    string num;
    cin >> n >> num;
    int len = num.length();

    char nums[len];
    for(int i = 0; i < len; i++){
        nums[i] = num[i];
    }

    vector<char> print;
    vector<vector<char>> answers(n+2, vector<char>(2*n+3, ' '));

    for(int i = 0; i < nums; i++){
        
    }






 
    return 0;
}